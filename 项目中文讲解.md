# Modbus 项目中文讲解

## 1. 项目概述

本项目演示了一套从 PC、Linux 开发板到 STM32 的完整 Modbus 控制链路。最终被控制的是 STM32F103VET6：主站可以设置板载 RGB LED、调整 PWM 占空比，并读取一个每秒递增的心跳值。

项目提供两种使用方式：

1. **Modbus RTU 直连模式**：LPB3568 作为 RTU 主站，通过 RS485 直接访问 STM32。
2. **Modbus TCP 网关模式**：PC 作为 TCP 主站访问 LPB3568，LPB3568 将 TCP 请求转换成 RTU 请求后访问 STM32。

```text
RTU 直连模式

LPB3568（RTU 主站） -- RS485 / Modbus RTU --> STM32（RTU 从站）


TCP 网关模式

PC（TCP 主站） -- Ethernet/Wi-Fi / Modbus TCP --> LPB3568（TCP-RTU 网关）
                                                       |
                                                       | RS485 / Modbus RTU
                                                       v
                                                STM32（RTU 从站）
```

这是一个纯 C 项目。PC 和 LPB3568 端使用 Makefile 构建；STM32 端使用 Keil 工程和 STM32F10x 标准外设库。

## 2. 目录结构

```text
modbus/
├─ ModbusRTU/
│  ├─ Stm32_modbus/             STM32F103 Modbus RTU 从站
│  │  ├─ App/                   主程序、协议栈、寄存器映射和中断
│  │  ├─ Drivers/BSP/           LED、PWM、RS485、SysTick 驱动
│  │  ├─ Drivers/CMSIS/         Cortex-M3 与 STM32 启动代码
│  │  ├─ Drivers/FWlib/         STM32F10x 标准外设库
│  │  └─ Project/               Keil 工程、编译产物和 HEX 文件
│  └─ LPB3568_RTU/              Linux 下的 Modbus RTU 主站
│     ├─ include/                模块接口
│     ├─ src/                    菜单、RTU 主站、CRC、串口实现
│     └─ Makefile
└─ ModbusTCP/
   ├─ MyPC/                     Windows/Linux Modbus TCP 主站
   │  ├─ include/
   │  ├─ src/
   │  └─ Makefile
   ├─ LPB3568_TCP/              Modbus TCP 到 RTU 的网关
   │  ├─ include/
   │  ├─ src/
   │  └─ Makefile
   ├─ README.md
   └─ 使用文档.md
```

`Project/Objects` 中的 `.o`、`.axf`、`.hex`、`.map` 等是 Keil 生成的构建产物，不是理解业务逻辑的重点。阅读代码时应优先看 `App`、`Drivers/BSP` 和各 Linux 程序的 `src`。

## 3. 三个角色及其职责

| 角色 | 程序 | 协议身份 | 主要职责 |
| --- | --- | --- | --- |
| STM32 | `ModbusRTU/Stm32_modbus` | RTU 从站 | 保存寄存器，校验请求，驱动 LED/PWM，返回响应 |
| LPB3568 RTU 工具 | `ModbusRTU/LPB3568_RTU` | RTU 主站 | 直接通过串口读写 STM32，适合先验证 RS485 链路 |
| LPB3568 网关 | `ModbusTCP/LPB3568_TCP` | TCP 服务端 + RTU 主站 | 拆解 Modbus TCP 帧，转发成 RTU 请求，再封装响应 |
| PC 工具 | `ModbusTCP/MyPC` | TCP 客户端/主站 | 连接网关，通过交互菜单远程读写 STM32 |

推荐先调通 RTU 直连模式，再加入 TCP 网关。这样遇到问题时可以快速判断故障位于串口侧还是网络侧。

## 4. STM32 寄存器映射

STM32 只定义了 3 个保持寄存器。代码位于 `App/register_map/register_map.h` 和 `register_map.c`。

| 协议地址 | 常见 4xxxx 表示法 | 含义 | 可用值 | 写入效果 |
| --- | --- | --- | --- | --- |
| `0` | `40001` | LED 模式 | `0..7` | 立即改变板载 RGB LED |
| `1` | `40002` | PWM 占空比 | `0..1000` | 写入 TIM3_CH1 比较值 |
| `2` | `40003` | 心跳计数 | `0..65535` 循环 | 主循环每秒自动加 1 |

注意：代码和报文使用的是**从 0 开始的协议地址**。有些 Modbus 调试软件使用 `40001` 风格显示地址，此时 `40001` 对应代码中的地址 `0`，不要误填成 `1`。

LED 模式定义如下：

| 值 | 颜色 | 值 | 颜色 |
| --- | --- | --- | --- |
| 0 | 关闭 | 4 | 黄色 |
| 1 | 红色 | 5 | 紫色 |
| 2 | 绿色 | 6 | 青色 |
| 3 | 蓝色 | 7 | 白色 |

PWM 使用 `0..1000` 表示 `0.0%..100.0%`。TIM3 初始化参数为 `ARR=1000`、`PSC=71`，在 72 MHz 定时器时钟下，频率约为：

```text
72 MHz / (71 + 1) / (1000 + 1) ≈ 999 Hz
```

PWM 从 PA6（TIM3_CH1）输出。RGB LED 使用 PB5、PB0、PB1，且为低电平点亮。

## 5. STM32 RTU 从站实现

### 5.1 启动与主循环

`App/main.c` 依次初始化 SysTick、LED、PWM、RS485 和 Modbus 从站。从站地址在代码中固定为 `1`，串口参数固定为 `9600 8N1`。

```text
上电
  -> 建立 1 ms SysTick
  -> 初始化 LED GPIO
  -> 初始化 TIM3 PWM
  -> 初始化 USART1 / RS485
  -> Modbus_Init(1)
  -> 循环调用 Modbus_Poll()
  -> 每 1000 ms 增加 heartbeat
```

RS485 使用 USART1：PA9 为 TX，PA10 为 RX。工程假设使用自动收发方向控制的 RS485 模块，因此代码没有单独操作 DE/RE 引脚。

### 5.2 接收与帧边界

USART1 开启两个中断：

- `RXNE`：每收到一个字节，就调用 `Modbus_ReceiveByte()` 放入 256 字节接收缓冲区。
- `IDLE`：总线出现空闲时，将 `g_modbus_frame_ready` 置 1，表示一帧接收完成。

主循环中的 `Modbus_Poll()` 发现完整帧后，会短暂关闭中断，把易变接收缓冲区复制到局部数组，然后重新开中断并解析。这样中断只负责快速收数，CRC 校验和业务处理留在主循环完成。

### 5.3 帧处理流程

`App/modbus_slave/modbus_slave.c` 的核心流程是：

```text
检查最小长度
  -> 检查从站地址（本机地址 1 或广播地址 0）
  -> 校验 CRC16
  -> 根据功能码分发
       0x03：读保持寄存器
       0x06：写单个保持寄存器
       0x10：写多个保持寄存器
  -> 普通请求返回响应；广播写请求执行但不响应
```

从站支持的异常码：

| 异常码 | 含义 | 典型原因 |
| --- | --- | --- |
| `0x01` | 非法功能 | 收到未实现的功能码 |
| `0x02` | 非法地址 | 地址越界，或写入值未通过当前寄存器校验 |
| `0x03` | 非法数据值 | 报文长度、数量等格式不合法 |

这里的一个实现细节是：LED 超过 7 或 PWM 超过 1000 时，`RegisterMap_WriteHolding()` 返回失败，最终回复 `0x02`。从语义上也可以将“值越界”设计为 `0x03`，但当前项目实际行为是 `0x02`。

### 5.4 寄存器与硬件解耦

协议模块不直接控制 GPIO 或 TIM，而是调用 `RegisterMap_ReadHolding()` 和 `RegisterMap_WriteHolding()`。写寄存器成功后，寄存器映射层再调用：

- `RegisterMap_ApplyLed()`：把 `0..7` 转换为 RGB GPIO 宏。
- `RegisterMap_ApplyPwm()`：调用 `Motor_SetSpeed()` 更新 TIM3 CCR1。

这种分层使协议解析与具体硬件控制相互独立。以后新增温度、继电器等数据时，主要扩展寄存器映射即可。

## 6. LPB3568 RTU 主站

`ModbusRTU/LPB3568_RTU` 是一个 POSIX/Linux 命令行程序，主要分为四层：

| 文件 | 作用 |
| --- | --- |
| `src/main.c` | 参数解析、交互菜单和 LED/PWM 业务操作 |
| `src/modbus_master.c` | 组 RTU 请求、收响应、校验地址/功能码/长度/CRC |
| `src/modbus_crc.c` | Modbus CRC16 |
| `src/serial_port.c` | termios 串口配置、带超时读取和完整写入 |

RTU 主站当前实现 `0x03` 和 `0x06`。一次事务的过程如下：

```text
清理串口残留数据
  -> 拼接 slave id、功能码、地址、数量/值
  -> 追加 CRC（低字节在前）
  -> 写入串口
  -> 最长等待响应首字节 500 ms
  -> 帧内字节间隔最多等待 20 ms
  -> 校验 CRC、从站地址、功能码和响应长度
  -> 将寄存器数据转换为 uint16_t
```

代码还会跳过可能由某些 RS485 硬件回送的请求回显。设置 LED 和 PWM 后，菜单程序会再次读取同一寄存器，确认实际值与写入值一致。

## 7. Modbus TCP 主站与网关

### 7.1 PC 端 TCP 主站

`ModbusTCP/MyPC` 与 RTU 主站的菜单基本相同，但底层帧改为 Modbus TCP：

- `src/main.c`：连接参数和交互菜单。
- `src/modbus_tcp_master.c`：MBAP 头、事务编号、PDU 和响应校验。
- `src/tcp_client.c`：兼容 Linux Socket 和 Windows Winsock。

Modbus TCP 不使用 CRC，因为 TCP 已提供传输校验。每个请求前有一个 7 字节 MBAP 头：

| 字段 | 长度 | 本项目处理方式 |
| --- | --- | --- |
| Transaction ID | 2 字节 | 每次请求递增，用于匹配响应 |
| Protocol ID | 2 字节 | 必须为 0 |
| Length | 2 字节 | 后续 Unit ID + PDU 的长度 |
| Unit ID | 1 字节 | 填 STM32 的 RTU 从站地址 |

### 7.2 LPB3568 TCP-RTU 网关

`ModbusTCP/LPB3568_TCP` 同时包含 TCP 服务端和 RTU 主站。一次读请求的转换过程如下：

```text
PC 发送 Modbus TCP ADU
  -> 网关读取并校验 MBAP 头
  -> 从 PDU 提取功能码、地址、数量
  -> 把 MBAP Unit ID 设置为 RTU slave id
  -> 调用 RTU 主站发送串口帧
  -> 收到并校验 STM32 RTU 响应
  -> 去掉 RTU 地址和 CRC，生成 TCP PDU
  -> 复用原 Transaction ID 返回 PC
```

网关只转发：

- `0x03`：读保持寄存器；
- `0x06`：写单个保持寄存器。

虽然 STM32 从站支持 `0x10`，当前网关和 PC 主站并未实现 `0x10` 转发。网关遇到其他功能码会回复 `0x01`。

如果 RTU 从站明确返回 `0x01..0x04` 异常，网关会映射为同名 TCP 异常；串口超时、CRC 错误等其他 RTU 故障统一转换为 `0x0B`（网关目标设备响应失败）。

当前网关采用同步、单进程、单客户端处理模型：`accept()` 一个连接后持续服务该连接，断开后才接受下一个连接。它适合教学和单客户端调试，不是面向高并发的生产网关。

## 8. 报文示例

假设从站地址为 1，读取地址 0 开始的 3 个保持寄存器。

RTU 请求：

```text
01 03 00 00 00 03 CRC_LO CRC_HI
|  |  |     |     |
|  |  |     |     +-- 数量 3
|  |  +-----+-------- 起始地址 0
|  +----------------- 功能码 0x03
+-------------------- 从站地址 1
```

如果 LED=红色、PWM=500、心跳=10，RTU 响应的数据部分为：

```text
01 03 06 00 01 01 F4 00 0A CRC_LO CRC_HI
         |     |     |
         |     |     +-- heartbeat = 10
         |     +-------- PWM = 500
         +-------------- LED = 1
```

写 LED 为蓝色的 RTU 请求为：

```text
01 06 00 00 00 03 CRC_LO CRC_HI
```

正常情况下 `0x06` 的响应会原样回显地址和值。

同一个 `0x03` 请求经过 Modbus TCP 时，功能码和数据字段不变，但从站地址进入 MBAP 的 Unit ID，帧尾不再有 CRC：

```text
[Transaction ID][00 00][Length][Unit ID=01][03 00 00 00 03]
```

## 9. 编译与运行

### 9.1 STM32 从站

1. 用 Keil 打开 `ModbusRTU/Stm32_modbus/Project/ModbusEx.uvprojx`。
2. 编译工程并烧录到 STM32F103VET6。
3. 将 USART1 对应的 RS485 接口连接到 LPB3568，确认 A/B 极性和共地。

仓库中已有构建出的 `Project/Objects/ModbusEx.hex`，但修改源码后应重新编译生成新的 HEX。

### 9.2 先运行 RTU 直连工具

在 LPB3568 上执行：

```sh
cd ModbusRTU/LPB3568_RTU
make
./lpb3568_modbus /dev/ttyS7 9600 1
```

交叉编译时可指定编译器：

```sh
make CC=aarch64-linux-gnu-gcc
```

参数依次为串口设备、波特率和默认从站地址。菜单可设置 LED、PWM，采集三个寄存器，也可按地址进行单寄存器读写。

### 9.3 运行 TCP-RTU 网关

在 LPB3568 上执行：

```sh
cd ModbusTCP/LPB3568_TCP
make
./lpb3568_modbus_tcp_gateway /dev/ttyS7 9600 1502
```

建议调试时使用 `1502`。标准端口 `502` 在 Linux 上通常需要 root 权限：

```sh
sudo ./lpb3568_modbus_tcp_gateway /dev/ttyS7 9600 502
```

不要同时运行 RTU 直连工具和 TCP 网关，因为它们会争用同一个串口设备。

### 9.4 运行 PC TCP 主站

Linux：

```sh
cd ModbusTCP/MyPC
make
./mypc_modbus_tcp_master <LPB3568-IP> 1502 1
```

Windows MinGW/MSYS2：

```sh
cd ModbusTCP/MyPC
mingw32-make
mypc_modbus_tcp_master.exe <LPB3568-IP> 1502 1
```

最后一个参数 `1` 会成为 Modbus TCP 的 Unit ID，并由网关转换为 STM32 的 RTU 从站地址。

## 10. 推荐调试顺序

1. 烧录 STM32，检查供电、RS485 A/B、共地和串口设备名。
2. 运行 `LPB3568_RTU`，先读取地址 2 两次，确认心跳递增。
3. 写地址 0 和地址 1，确认 LED 与 PA6 PWM 有真实硬件变化。
4. 停止 RTU 工具，启动 `LPB3568_TCP` 网关。
5. 从 PC `ping` 通 LPB3568，并确认 TCP 端口可访问。
6. 启动 `MyPC`，再完成相同的读写测试。

任一 Linux/PC 程序都可用 `MODBUS_DEBUG=1` 输出十六进制帧。例如：

```sh
MODBUS_DEBUG=1 ./lpb3568_modbus /dev/ttyS7 9600 1
MODBUS_DEBUG=1 ./lpb3568_modbus_tcp_gateway /dev/ttyS7 9600 1502
MODBUS_DEBUG=1 ./mypc_modbus_tcp_master 192.168.1.30 1502 1
```

PowerShell 中设置方式为：

```powershell
$env:MODBUS_DEBUG = "1"
.\mypc_modbus_tcp_master.exe 192.168.1.30 1502 1
```

## 11. 常见问题

### 11.1 RTU 完全无响应

依次检查串口是否正确、波特率是否为 9600、从站地址是否为 1、A/B 是否接反、是否共地，以及 RS485 模块是否支持自动方向控制。当前 STM32 代码不负责控制 DE/RE，引脚方向控制型模块需要额外适配。

### 11.2 能连接 TCP，但所有请求返回 `0x0B`

TCP 链路已经通，问题通常在网关到 STM32 的 RTU 链路。查看网关输出的 `RTU read failed` 或 `RTU write failed`，并打开 `MODBUS_DEBUG` 检查串口 TX/RX。

### 11.3 返回 `0x02` 或 PC 显示 `-2`

访问地址必须落在 `0..2`。写 LED 必须为 `0..7`，PWM 必须为 `0..1000`。当前实现中写入越界值也会返回 `0x02`。

### 11.4 心跳不增长

心跳由 STM32 的 1 ms SysTick 和主循环共同维护。若一直为 0，应检查固件是否运行、程序是否陷入 Fault Handler，以及 SysTick 时钟配置是否正常。

### 11.5 第一个客户端不退出，第二个客户端连上却不能通信

网关一次只处理一个客户端。先退出或断开第一个 PC 主站，网关才会回到 `accept()` 服务下一个客户端。

### 11.6 中文注释或原使用文档显示乱码

仓库中部分中文内容可能存在编码不一致。建议编辑器使用 UTF-8 打开本讲解文档；对旧文件可尝试以 GBK/GB2312 重新打开后再统一转为 UTF-8。

## 12. 阅读源码的建议顺序

第一次阅读时，可按下面的调用链推进：

```text
STM32：
App/main.c
  -> App/stm32f10x_it.c
  -> App/modbus_slave/modbus_slave.c
  -> App/register_map/register_map.c
  -> Drivers/BSP/bsp_rs485.c、bsp_led.c、bsp_pwm.c

RTU 主站：
LPB3568_RTU/src/main.c
  -> modbus_master.c
  -> serial_port.c
  -> modbus_crc.c

TCP 链路：
MyPC/src/main.c
  -> modbus_tcp_master.c
  -> tcp_client.c
  -> LPB3568_TCP/src/main.c
  -> modbus_master.c
  -> serial_port.c
```

理解这个项目最重要的主线是：**菜单中的一次业务操作，如何变成 Modbus PDU，如何经过 TCP/RTU 封装传输，最后如何落到 STM32 的寄存器写入和硬件动作上。**

## 13. 当前实现边界与可扩展方向

当前代码适合学习和实验，扩展为实际工程时可以考虑：

- 网关增加 `0x10` 转发，与 STM32 已有的多寄存器写能力对齐；
- 网关引入 `poll`/`epoll` 或线程模型，支持多个 TCP 客户端；
- STM32 使用严格的 RTU 3.5 字符静默时间判帧，而不只依赖 USART IDLE；
- 若 RS485 模块不是自动方向控制，增加 DE/RE GPIO 切换和发送完成等待；
- 把从站地址、波特率等参数做成 Flash 配置，而不是编译期固定值；
- 为协议解析、CRC、异常映射和寄存器边界增加自动化测试；
- 清理或忽略 Keil 构建产物，避免二进制文件频繁进入版本变更。

